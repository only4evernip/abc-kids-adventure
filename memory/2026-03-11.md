# 2026-03-11

- 今天完成了两个本地 skill 的系统性优化：`~/.openclaw/skills/agent-reach/SKILL.md` 与 `~/.openclaw/skills/garmin-to-strava/SKILL.md`。
- `agent-reach` 这轮重点：收紧 frontmatter `description` 的触发边界；把正文从“工具清单”改成“联网入口 + routing guide”；补了两套评估样本：
  - `/tmp/agent-reach-evals/evals.json`
  - `/tmp/agent-reach-evals/description-optimization-evals.json`
- `garmin-to-strava` 这轮重点：把 description 改成可触发描述；把 `SKILL.md` 重构成执行型工作流；把重知识内容迁到 `references/advanced.md`；补了两套评估样本：
  - `/tmp/garmin-to-strava-evals/evals.json`
  - `/tmp/garmin-to-strava-evals/description-optimization-evals.json`
- 这次尝试把技能改动提交到 `~/.openclaw/workspace` 的 git 仓库时，发现 skill 文件不在该 repo 下，无法作为 workspace commit 管理；中途产生过一次脏 commit，随后已撤销，结论是：`workspace` repo 不能直接承载 `~/.openclaw/skills/...` 的提交历史。
- 基于一篇关于 OpenClaw「养小龙虾」的公众号文章，和用户一起抽象出“阿肖版 3 只小龙虾”框架：
  1. 选品侦察虾（发现机会、需求、竞争、风险）
  2. 数据操盘虾（结构化沉淀到飞书文档/多维表）
  3. 决策军师虾（基于已有数据做 go/no-go 与优先级判断）
- 当前优先落地的是“选品侦察虾”，并把市场范围明确收窄为：仅做美国与加拿大市场（US/CA）。
- 已为“选品侦察虾”整理出一套适用于 Amazon US / Amazon CA 的调研框架：关键词拆分、Keepa 需要观察的字段、风险判断逻辑、以及飞书多维表字段设计。
- 用户给出的真实测试产品词是 `mouth guard for sleep`；初筛结论是：原词不建议直接做，需求存在但定义混乱、竞争重、合规/功效/舒适度风险高；建议拆成更细方向，其中用户选择继续研究“止鼾口腔装置（anti snoring mouthpiece / snoring mouth guard / mandibular advancement device）”。
- 当前已确认用户有 Keepa API，可作为后续 Amazon 选品侦察的重要数据源。
- 今天还和用户一起折腾了 Feishu 多维表权限。当前已确认打开的关键权限包括：
  - `bitable:app`
  - `bitable:app:readonly`
  - `base:app:create`
  - `base:field:create/read/update`
  - `base:record:create/read/retrieve/update`
- 尽管权限已齐，`feishu_bitable_create_app` 依然返回 400；后续改为让用户手动创建一张 wiki 挂载的多维表。浏览器可以访问该表，但 `feishu_bitable_get_meta` 对该 wiki 链接仍返回 400，说明当前工具链无法稳定从 wiki 挂载链接解析出可用的 bitable app token。
- 结论：后续若继续落地飞书多维表，优先方案不是继续卡在自动建表，而是：
  1. 先在本地生成 CSV/结构化模板
  2. 再导入飞书
  3. 或者用户只手动建最小字段集，后续再补数据
- 晚上继续把“选品侦察网页”从空想拉到可落地规则包，直接在 workspace 新建了 `ecom-scout-dashboard/`：
  - 已基于真实样本 `候选品侦察池_US_CA.csv` 提取 24 列原始字段，并固化成字段映射文档：`docs/02-field-mapping.md`
  - 已落地前端技术架构文档：`docs/01-frontend-architecture.md`
  - 已落地核心评分规则：`docs/03-rps-rules-v1.md`
  - 已落地流转状态机：`docs/04-workflow-state-machine.md`
  - 已补最小代码骨架：`src/types/product.ts`、`src/lib/fieldMap.ts`、`src/domain/rps.ts`、`src/store/useScoutStore.ts`、`src/workers/score.worker.ts`，以及最小可跑壳子（`package.json` / `vite.config.ts` / `index.html` / `src/main.tsx` / `src/App.tsx`）
- 随后用户把整套规则包交给 Gemini 做技术/架构复审，关键批评点非常准：
  1. Zustand 不该存全量大数组，Dexie 应该变成主数据仓
  2. `riskMultiplier`、`confidenceMultiplier`、`overheatingPenalty` 三套惩罚逻辑叠加，会导致规则重叠和分数失真
  3. MVP 不该先做图表和拖拽看板，而应先打穿表格主链路
- 已按 Gemini 意见完成一轮“减法重构”：
  - `README.md` 改成 Gemini 评审后重构版
  - 前端架构改为：**Dexie 主仓 + Zustand 仅存 UI 状态 + Worker 负责评分并直接入库**
  - 新增：`src/lib/db.ts`、`src/lib/csvSchema.ts`
  - `package.json` 加入 `dexie-react-hooks`，移除对 MVP 无关的图表依赖
  - `src/domain/rps.ts` 改为：**Eligibility Gate + 线性打分 + 固定风险扣分 + 标签 + 状态分流**
  - 删除分数层面的 `riskMultiplier` / `confidenceMultiplier` / `overheatingPenalty`，改为：
    - 高风险 = 固定扣分 + 状态上限
    - 缺字段 = `待补证`
    - 过热 = Tag
    - 数据过期 = Tag + 进入待补证
  - `src/store/useScoutStore.ts` 重构为只存：filters / selectedProductId / drawer 状态 / import 进度等轻状态
  - `src/workers/score.worker.ts` 重构为：worker 内计算 RPS 并 `bulkPut` 入 Dexie，只回传摘要消息给主线程
- 这一轮之后，`ecom-scout-dashboard/` 已从“概念规则包”变成“更接近真实可开发 v1 的前端底座”；下一步最自然的是把 PapaParse + Zod 真的接到 CSV，再用 `useLiveQuery` + TanStack Table 跑第一版发现矩阵。
- 晚上后续又把这一步真正落地了：
  - 已把 `bigCategory` 真正接进 `csvSchema.ts` / `fieldMap.ts`
  - 已把 Dexie 升级为带复合索引的主仓（含 `[workflowStatus+rps.score.finalScore]` 和 `[market+workflowStatus]`）
  - 已把 `score.worker.ts` 改成 **PapaParse + Zod + RPS + Dexie bulkPut** 的真实导入链路，并加入稳定 ID 生成与 progress 回传
  - 已把 `Date.parse` 风险收敛到 schema 层，优先统一成标准 `YYYY-MM-DD`，并兼容 Excel serial date
  - `src/App.tsx` 已补最小可用导入页：支持手动选择 CSV，也支持“导入内置样本”按钮；页面会显示导入进度、批次信息、本地记录数和最近导入记录
  - 已执行 `npm install`、`npm run typecheck`、`npm run build`，均通过
  - 已通过浏览器做了一次端到端烟测：点击“导入内置样本”后，样本 CSV 成功进入 Dexie，本地记录数变为 1，行内能看到 `美国 / anti snoring mouthpiece / 止鼾口腔装置 / RPS 21.5 / 待补证`
- 目前 `ecom-scout-dashboard/` 已不再只是规则包，而是一个 **能真实导入 CSV、评分、入本地库并在页面回显结果的 MVP 骨架**。
- 之后又继续把第一版“发现矩阵”首屏补上了，不再只有导入页：
  - `src/App.tsx` 现在已经包含：导入区、导入状态区、左侧筛选器、中间发现矩阵表格、右侧详情抽屉
  - 发现矩阵使用 `@tanstack/react-table`，支持前端排序，字段包括：市场、关键词、产品方向、RPS、状态、风险、标签、导入时间
  - 左侧筛选器支持：关键词、市场、状态、风险、最低/最高 RPS
  - 右侧详情抽屉会展示：Eligibility Gate、评分拆解、数据质量、VOC、系统标签、建议动作
  - 页面仍坚持 MVP 原则：先不做图表，先把“导入 -> 评分 -> 表格筛选 -> 点开详情”打穿
- 这一步已经再次通过本地工程验证：重新 `npm install` 后，`npm run typecheck` 与 `npm run build` 均通过；说明从规则包、导入闭环到发现矩阵首屏，这条线已经能连续编译通过。
- 随后又根据 Gemini 三审继续做了两刀高优修复：
  1. **修 Dexie 查询下推 bug**：不再先 `limit(1000)` 再在内存里 `filter`，而是新增 `src/lib/productQuery.ts`，把筛选条件真正下推到 Dexie 查询层，再由 UI 消费结果；这样不会再发生“第 1001 条以后永远筛不出来”的业务级 bug。
  2. **拆 `App.tsx`**：新增 `src/components/import/ImportSection.tsx`、`src/components/discovery/FilterSidebar.tsx`、`src/components/discovery/ProductTable.tsx`、`src/components/detail/DetailDrawer.tsx`，并新增 `src/hooks/useScoreWorker.ts`；`App.tsx` 现在只负责组装，不再继续膨胀成 God Component。
- 另外顺手还做了两项工程增强：
  - `score.worker.ts` 从传 `csvText` 改成传 `File`，把解析动作彻底收进 Worker，避免主线程先把整个 CSV 读成大字符串
  - `db.ts` 升到 version 3，补了 `overallRisk` 索引；筛选项选项列表也改为从 Dexie 的索引/unique keys 取值
- 本轮修改后，再次执行了 `npm install`、`npm run typecheck`、`npm run build`，全部通过。当前 `ecom-scout-dashboard/` 更接近一个可持续扩展的正式工作台底座。
- 用户随后直接拍板要把我刚列的 1-2-3 全做（虚拟滚动 / session 导出导入 / 状态修改与本地备注），这轮已经一口气补上：
  1. **虚拟滚动**：`src/components/discovery/ProductTable.tsx` 引入 `@tanstack/react-virtual`，发现矩阵表格现在是虚拟滚动，不再一次性把整页 DOM 全渲染死。
  2. **session 导出/导入**：`src/lib/db.ts` 新增 `exportSessionPayload()` 与 `importSessionPayload()`，`ImportSection.tsx` 新增“导出 Session JSON / 导入 Session JSON”入口。
  3. **状态修改 + 本地备注**：`DetailDrawer.tsx` 新增 workflow status 下拉与 notes 文本框，并可保存到 Dexie；`db.ts` 新增 `updateProductRecord()`。
- 这轮还顺手做了两项重要工程收束：
  - `useScoreWorker.ts` 现在同时负责 worker 生命周期与导入消息，不再让 `App.tsx` 管过多细节
  - `score.worker.ts` 继续保持 File 直接进入 Worker 的路径，避免主线程先把 CSV 整体读成大字符串
- 最新工程状态：`ecom-scout-dashboard/` 已具备 **导入 CSV / 导入导出 Session / Worker 打分 / Dexie 入库 / 发现矩阵筛选排序 / 右侧详情修改状态与备注 / 虚拟滚动表格** 这一整套 MVP 工作台能力。
- 用户今天一个很明确的协作偏好是：**先让我按可落地思路做第一版，再把文档/规则包丢给 Gemini 做第二轮“挑错式复审”**。这不是随口一说，已经在“选品侦察网页”上验证过一次，Gemini 的作用更适合做减法重构与技术审查，而不是从 0 起草业务方案。
- 今天另一条重要产出是 `Master` 里的指数策略重构：围绕 Gemini 的反馈，连续做了三步改造。
  1. **Step 1**：ETF 池扩充 + 环境阈值相对化（加入黄金 ETF / 美元债 QDII，成交额从绝对值改为相对强度）
  2. **Step 2**：状态确认期 + EWMA 仓位平滑（连续 2 天确认跨档、单日单桶最大变动 10%）
  3. **Step 3**：偏离度触发 + 熔断 + ETF 优选（Tracking Error、连续防守触发熔断、动态 ETF 推荐）
- 指数策略这轮关键认知已经明确：
  - 不能再用连板/炸板这类投机情绪指标去决定宽基指数配置
  - 应改用 **Beta 动量 + 广度 + 量能相对强度**
  - 大切换不能断崖式调仓，必须渐进式
  - 熔断和偏离度约束应该优先于“想当然的全仓切换”
- 今天新增/重要提交（便于后续追溯）：
  - `d502021`：新增 `ecom-scout-dashboard/` 规则包与前端骨架
  - `52b93d6`：按 Gemini 复审意见对 `ecom-scout-dashboard/` 做减法重构
  - `cc916f3` / `78f123f` / `e49afa2`：`Master/index_strategy.py` 及相关规则文档三步重构
