# 2026-03-11

- 今天完成了两个本地 skill 的系统性优化：`~/.openclaw/skills/agent-reach/SKILL.md` 与 `~/.openclaw/skills/garmin-to-strava/SKILL.md`。
- `agent-reach` 这轮重点：收紧 frontmatter `description` 的触发边界；把正文从“工具清单”改成“联网入口 + routing guide”；补了两套评估样本：
  - `/tmp/agent-reach-evals/evals.json`
  - `/tmp/agent-reach-evals/description-optimization-evals.json`
- `garmin-to-strava` 这轮重点：把 description 改成可触发描述；把 `SKILL.md` 重构成执行型工作流；把重知识内容迁到 `references/advanced.md`；补了两套评估样本：
  - `/tmp/garmin-to-strava-evals/evals.json`
  - `/tmp/garmin-to-strava-evals/description-optimization-evals.json`
- 这次尝试把技能改动提交到 `~/.openclaw/workspace` 的 git 仓库时，发现 skill 文件不在该 repo 下，无法作为 workspace commit 管理；中途产生过一次脏 commit，随后已撤销，结论是：`workspace` repo 不能直接承载 `~/.openclaw/skills/...` 的提交历史。
- 基于一篇关于 OpenClaw「养小龙虾」的公众号文章，和用户一起抽象出“阿肖版 3 只小龙虾”框架：
  1. 选品侦察虾（发现机会、需求、竞争、风险）
  2. 数据操盘虾（结构化沉淀到飞书文档/多维表）
  3. 决策军师虾（基于已有数据做 go/no-go 与优先级判断）
- 当前优先落地的是“选品侦察虾”，并把市场范围明确收窄为：仅做美国与加拿大市场（US/CA）。
- 已为“选品侦察虾”整理出一套适用于 Amazon US / Amazon CA 的调研框架：关键词拆分、Keepa 需要观察的字段、风险判断逻辑、以及飞书多维表字段设计。
- 用户给出的真实测试产品词是 `mouth guard for sleep`；初筛结论是：原词不建议直接做，需求存在但定义混乱、竞争重、合规/功效/舒适度风险高；建议拆成更细方向，其中用户选择继续研究“止鼾口腔装置（anti snoring mouthpiece / snoring mouth guard / mandibular advancement device）”。
- 当前已确认用户有 Keepa API，可作为后续 Amazon 选品侦察的重要数据源。
- 今天还和用户一起折腾了 Feishu 多维表权限。当前已确认打开的关键权限包括：
  - `bitable:app`
  - `bitable:app:readonly`
  - `base:app:create`
  - `base:field:create/read/update`
  - `base:record:create/read/retrieve/update`
- 尽管权限已齐，`feishu_bitable_create_app` 依然返回 400；后续改为让用户手动创建一张 wiki 挂载的多维表。浏览器可以访问该表，但 `feishu_bitable_get_meta` 对该 wiki 链接仍返回 400，说明当前工具链无法稳定从 wiki 挂载链接解析出可用的 bitable app token。
- 结论：后续若继续落地飞书多维表，优先方案不是继续卡在自动建表，而是：
  1. 先在本地生成 CSV/结构化模板
  2. 再导入飞书
  3. 或者用户只手动建最小字段集，后续再补数据
- 晚上继续把“选品侦察网页”从空想拉到可落地规则包，直接在 workspace 新建了 `ecom-scout-dashboard/`：
  - 已基于真实样本 `候选品侦察池_US_CA.csv` 提取 24 列原始字段，并固化成字段映射文档：`docs/02-field-mapping.md`
  - 已落地前端技术架构文档：`docs/01-frontend-architecture.md`，明确采用 Vite + React + TypeScript + Zustand + PapaParse + Zod + TanStack Table + ECharts + Dexie 的本地前端方案
  - 已落地核心评分规则：`docs/03-rps-rules-v1.md`，包括 Eligibility Gate、一票否决、RPS 基础分、风险乘数、置信度乘数、过热惩罚、动作分层
  - 已落地流转状态机：`docs/04-workflow-state-machine.md`
  - 已补最小代码骨架：`src/types/product.ts`、`src/lib/fieldMap.ts`、`src/domain/rps.ts`、`src/store/useScoutStore.ts`、`src/workers/score.worker.ts`，以及最小可跑壳子（`package.json` / `vite.config.ts` / `index.html` / `src/main.tsx` / `src/App.tsx`）
- 这轮判断是：先把规则、字段、状态机、评分骨架定死，比先堆 Dashboard 更值钱；后续最自然的下一步是把 PapaParse + Zod 真接上 CSV，再把 `calculateRps()` 挂进 worker。 
